import { Posts } from "./posts/Posts";

export const Profile = () => {
    return (
        <div>
            PROFILE
            <img alt='background' />
            <Posts />
        </div>
    );
};
